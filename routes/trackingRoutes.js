const express = require('express');
//const { trackCurriculumRegistration, trackModuleProgression ,trackLessonInteraction, trackAssessmentAttempt, trackResourceAccess, trackLearningPathwayProgression,getStatements } = require('../controllers/trackingController');

const router = express.Router();



module.exports = router;

//const express = require('express');
// const { processChatHistory } = require('../chatProcessor');

// const router = express.Router();

// // Endpoint to trigger chat processing manually
// router.get('/process-chats', async (req, res) => {
//   try {
//     await processChatHistory();
//     res.status(200).send('Chat processing completed');
//   } catch (err) {
//     console.error('Error processing chats:', err);
//     res.status(500).send('An error occurred while processing chats');
//   }
// });

// module.exports = router;
