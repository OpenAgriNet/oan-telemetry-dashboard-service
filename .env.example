# Server
PORT=3000

# PostgreSQL
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=telemetry
DB_POOL_MAX=20
DB_IDLE_TIMEOUT_MS=30000
DB_CONN_TIMEOUT_MS=5000

# Keycloak JWKS – set Option A or Option B
# Option A: full JWKS URL
# KEYCLOAK_JWKS_URI=https://auth.example.com/realms/your-realm/protocol/openid-connect/certs

# Option B: base URL + realm (JWKS URL is derived)
KEYCLOAK_URL=https://auth.example.com
KEYCLOAK_REALM=your-realm
